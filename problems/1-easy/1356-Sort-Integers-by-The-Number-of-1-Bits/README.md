Use custom comparison sort.

Note if you want stable sort use SortStableFunc.

Use bits.OnesCount() to find ones count for an unsigned int. Much faster than converting it to a string binary representation then counting