dfs traverse left half by visiting left child first, compare with dfs traverse right half by visiting right child first, then compare if they are the same.
- Or just traverse entire tree in both ways and compare

Or use BFS, then compare level by level to see if mirrored.